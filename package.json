{
  "name": "syncfin",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "wrangler-deploy": "npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "typescript": "5.6",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@jellyfin/sdk": "^0.11.0"
  }
}
